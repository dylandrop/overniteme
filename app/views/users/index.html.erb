<%= will_paginate @users %>
<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.username, user %>
      <%= user.description %>
      <%= link_to "Message #{user.username}", new_conversation_path(user: user) %>
    </li>
  <% end %>
</ul>